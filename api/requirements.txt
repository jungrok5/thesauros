# Vercel Python serverless deps for /api/us-analysis
# Subset of repo-root requirements.txt — only what the analyze pipeline
# touches at runtime.

pandas==2.2.0
numpy==1.26.4
# scipy intentionally OMITTED — Vercel Python serverless 250MB cap
# can't fit pandas+numpy+scipy together. app/book/_swings replaced
# scipy.signal.argrelextrema with a numpy-only equivalent (2026-05-24).
requests==2.31.0
python-dateutil==2.8.2
psycopg[binary]>=3.2
# app.db.connection uses psycopg_pool.ConnectionPool — without this the
# Vercel Python function fails at import with `No module named 'psycopg_pool'`
# before our handler ever runs (silent HTML 500 instead of JSON traceback).
psycopg-pool>=3.2
