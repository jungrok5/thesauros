# Python
.venv/
.cache/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.env
.DS_Store

# Large data files (PIT database, model artifacts)
data/*.duckdb
data/*.parquet
models_store/*.pkl
models_store/*.parquet
models_store/signal_cache/

# Misc temp files (Claude task outputs misplaced as filenames)
C:Temp*.txt
.claude/settings.local.json

# Book image originals (large jpegs — keep transcripts/summaries/charts only)
book_images/_dl_all/
book_images/_ordered/
book_images/_zip_check/
book_images/_missing_pages/
book_images/**/*.jpg
book_images/**/*.jpeg
book_images/**/*.png

# Re-include curated artifacts
!book_images/**/transcripts/
!book_images/**/transcripts/**
!book_images/**/charts/
!book_images/**/charts/**
!book_images/**/SUMMARY*.md
!book_images/**/*.md
!book_images/toc/*.md
!book_images/toc/*.txt

# Legacy v1 archives (not maintained)
_archive_v1/
_archive_v1_web/

# IDE
.vscode/
.idea/

# telegram bot long-poll offset state (runtime, per-machine)
app/db/telegram_bot_state.txt

# Personal working docs (local-only)
WORK_LOG.md
TODO.md
PLAN_BOOK_SITE.md

# Book transcript recovery script — references session log paths
# and content-recovery flow tied to copyright-cleaned material.
scripts/restore_book_transcripts.py

# Book transcripts/charts/SUMMARY — kept on disk for reference but
# never tracked (recovered from session JSONL via the recovery script).
book_images/**/transcripts/
book_images/**/charts/
book_images/**/SUMMARY*.md
