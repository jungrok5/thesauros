"""CLI commands for the book analysis system.

Usage:
  python -m app.cli macro              — show current macro snapshot
  python -m app.cli analyze AAPL       — full book analysis on a ticker
  python -m app.cli ingest-macro       — refresh macro indicators from FRED+yfinance
  python -m app.cli ingest-krx         — refresh KOSPI200+KOSDAQ150 prices
"""
