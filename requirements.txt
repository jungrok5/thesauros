# Thesauros — Python dependencies (cron + bot only; no HTTP server)
#
# Used by:
#   - cron scripts in app/db/* (scan_daily, publish_macro, publish_chart,
#     telegram_*, ingest_themes, ingest_investor_flow, seed_tickers, migrate)
#   - data ingest modules (DART, KRX, KIS, SEC)
#   - book analyzer (candles/patterns/trend)
#
# No FastAPI/uvicorn/lightgbm/scikit-learn — site runs on Vercel + Supabase.

# Data + analysis
pandas==2.2.0
numpy==1.26.4
scipy>=1.10
requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0
python-dateutil==2.8.2
joblib>=1.4
tqdm>=4.66

# Market data sources
yfinance==0.2.40
pykrx>=1.0.45
finance-datareader>=0.9.96
fredapi>=0.5.1

# Local cache / staging (PIT db — slated for removal in Phase 7)
duckdb>=1.4

# Supabase + DB layer (cron writes here)
supabase>=2.30
psycopg[binary]>=3.2
psycopg-pool>=3.2

# Telegram bot + web-push delivery
python-dotenv>=1.0
cryptography>=42                # VAPID key generation
pywebpush>=2                    # optional — webpush.py is a no-op without it
